<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE artifacts>

<artifacts>
	<artifact packageName="br.com.innovativethinking.framework.service.util.ServiceUtil.getWebServiceInterfacePackageByModel(#{clazz})"
	          name="br.com.innovativethinking.framework.service.util.ServiceUtil.getWebServiceInterfaceNameByModel(#{clazz})">
		package @{packageName};

		import #{clazz.name};
		
		import br.com.innovativethinking.framework.service.interfaces.IService;
		
		/**
		 * Interface that defines the web service of the #{clazz}.
		 *
		 * @author @{user.name}
		 * @version @{build.version}
		 * @since @{now}
		 */
		public interface @{name} extends IService&lt;#{clazz.simpleName}&gt;{
		}
	</artifact>
</artifacts>