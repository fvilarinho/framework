<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE artifacts>

<artifacts>
	<artifact packageName="br.com.innovativethinking.framework.controller.form.util.ActionFormUtil.getActionPackageByModel(#{clazz})"
	          name="br.com.innovativethinking.framework.controller.form.util.ActionFormUtil.getActionNameByModel(#{clazz})">
		package @{packageName};
		
		import #{clazz.name};
		
		/**
		 * Class that defines the actions of the main console UI.
		 *
		 * @author @{user.name}
		 * @version @{build.version}
		 * @since @{now}
		 */
		public class @{name} extends br.com.innovativethinking.framework.controller.action.MainConsoleAction&lt;#{clazz.simpleName}&gt;{
		}
	</artifact>
</artifacts>